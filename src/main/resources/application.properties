spring.application.name=reseau-social-universitaire
server.port=8080

# ==========================================
# SUPABASE POSTGRESQL - SESSION POOLER
# ==========================================
spring.datasource.url=jdbc:postgresql://aws-0-eu-west-2.pooler.supabase.com:5432/postgres
spring.datasource.username=postgres.eerlsgtqzkzjbhalsfat
spring.datasource.password=reseau-social-2026
spring.datasource.driver-class-name=org.postgresql.Driver

# ==========================================
# JPA / HIBERNATE
# ==========================================
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ==========================================
# HIKARI
# ==========================================
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000

# ==========================================
# JWT (requis par JwtService — @Value sans valeur par défaut)
# ==========================================
app.jwt.secret=ReseauSocialUniversitaireSecretKey2026SecureJwtTokenKeyUniversite32CharsLong
app.jwt.expiration-ms=604800000

# ==========================================
# UPLOAD D'IMAGES
# ==========================================
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
